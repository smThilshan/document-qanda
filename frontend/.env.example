# Copy this file to .env (for local dev) and set the real value in Vercel's
# dashboard (for deployment). Vite only exposes env vars prefixed VITE_ to
# the browser bundle — never put a secret here, since anything with this
# prefix ends up readable in the shipped JS.

VITE_API_URL=http://localhost:8000
